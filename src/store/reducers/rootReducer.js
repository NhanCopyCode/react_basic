const initState = {
    users: [
        { id: 1, name: "Nhangg" },
        { id: 2, name: "Hoi dan IT" },
    ],
};
const rootReducer = (state = initState, action) => {

    return state;
}

export default rootReducer;